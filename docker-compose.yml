services:
  fund-exporter:
    build: .
    ports:
      - "${EXPORTER_PORT:-8000}:${EXPORTER_PORT:-8000}"
    env_file: .env
    environment:
      - TZ=Asia/Taipei
    init: true
    restart: unless-stopped
